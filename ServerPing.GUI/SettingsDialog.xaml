<Window x:Class="ServerPing.GUI.SettingsDialog"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="设置"
        Height="320" Width="400"
        WindowStyle="None"
        ResizeMode="NoResize"
        WindowStartupLocation="CenterOwner">

    <WindowChrome.WindowChrome>
        <WindowChrome CaptionHeight="0" ResizeBorderThickness="0" GlassFrameThickness="0" NonClientFrameEdges="None" />
    </WindowChrome.WindowChrome>

    <Border Background="{StaticResource AppBackgroundBrush}"
            BorderBrush="{StaticResource BorderBrushDark}" BorderThickness="1">
        <Grid>
            <Grid.RowDefinitions>
                <RowDefinition Height="42" />
                <RowDefinition Height="*" />
                <RowDefinition Height="Auto" />
            </Grid.RowDefinitions>

            <!-- Title bar -->
            <Grid Grid.Row="0" Background="{StaticResource TitleBarBrush}"
                  MouseLeftButtonDown="TitleBar_MouseLeftButtonDown">
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="*" />
                    <ColumnDefinition Width="Auto" />
                </Grid.ColumnDefinitions>
                <TextBlock Text="设置" FontSize="13" FontWeight="SemiBold" Margin="14,0,0,0" />
                <Button Grid.Column="1" Style="{StaticResource TitleBarCloseButton}"
                        Width="38" Height="38" Click="Close_Click">
                    <Path Data="{StaticResource Icon.X}"
                          Stroke="{Binding Foreground, RelativeSource={RelativeSource AncestorType=Button}}"
                          StrokeThickness="1.6" StrokeLineJoin="Round"
                          StrokeStartLineCap="Round" StrokeEndLineCap="Round"
                          Width="13" Height="13" Stretch="Uniform" />
                </Button>
            </Grid>

            <!-- Content -->
            <StackPanel Grid.Row="1" Margin="18,16,18,0">
                <TextBlock Text="监控" FontWeight="SemiBold" Foreground="{StaticResource MutedTextBrush}"
                           FontSize="11" Margin="0,0,0,12" />

                <Grid Margin="0,0,0,10">
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="160" />
                        <ColumnDefinition Width="*" />
                    </Grid.ColumnDefinitions>
                    <TextBlock Text="Ping 间隔（秒）" VerticalAlignment="Center" />
                    <TextBox Grid.Column="1" x:Name="PingIntervalTextBox" />
                </Grid>

                <Grid Margin="0,0,0,14">
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="160" />
                        <ColumnDefinition Width="*" />
                    </Grid.ColumnDefinitions>
                    <TextBlock Text="失败通知次数" VerticalAlignment="Center" />
                    <TextBox Grid.Column="1" x:Name="FailureThresholdTextBox" />
                </Grid>

                <CheckBox x:Name="SilentStartupCheckBox"
                          Content="静默启动，不自动打开管理面板"
                          Margin="0,0,0,16" />

                <!-- Test notification -->
                <Border Background="{StaticResource PanelBackgroundBrush}"
                        BorderBrush="{StaticResource BorderBrushDark}"
                        BorderThickness="1" CornerRadius="5" Padding="12,10">
                    <Grid>
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="*" />
                            <ColumnDefinition Width="Auto" />
                        </Grid.ColumnDefinitions>
                        <StackPanel>
                            <TextBlock Text="测试通知" FontWeight="SemiBold" />
                            <TextBlock Text="发送一条 Windows Toast 测试消息"
                                       Foreground="{StaticResource MutedTextBrush}"
                                       FontSize="12" Margin="0,3,0,0" />
                        </StackPanel>
                        <Button Grid.Column="1" Content="测试" Padding="12,5"
                                Click="TestNotification_Click" />
                    </Grid>
                </Border>

                <TextBlock x:Name="MessageTextBlock"
                           Foreground="{StaticResource MutedTextBrush}"
                           FontSize="12" Margin="0,8,0,0" />
            </StackPanel>

            <!-- Footer buttons -->
            <Border Grid.Row="2"
                    BorderBrush="{StaticResource BorderBrushDark}" BorderThickness="0,1,0,0"
                    Padding="14,10">
                <StackPanel Orientation="Horizontal" HorizontalAlignment="Right">
                    <Button Content="取消" Padding="16,5" Margin="0,0,8,0" Click="Close_Click" />
                    <Button Content="保存" Padding="16,5"
                            Background="{StaticResource AccentBrush}"
                            BorderBrush="{StaticResource AccentBrush}"
                            Click="Save_Click" />
                </StackPanel>
            </Border>
        </Grid>
    </Border>
</Window>
