<Window x:Class="ServerPing.GUI.ImportDialog"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="从 Windows Terminal 导入 SSH Profile" Height="400" Width="550"
        WindowStartupLocation="CenterOwner" ResizeMode="CanResizeWithGrip">

    <Grid Margin="12">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="*" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
        </Grid.RowDefinitions>

        <TextBlock Grid.Row="0" Text="选择要导入的 SSH Profile:" Margin="0,0,0,8" />

        <DataGrid Grid.Row="1" x:Name="ProfileGrid" AutoGenerateColumns="False"
                  CanUserAddRows="False" CanUserDeleteRows="False" IsReadOnly="False"
                  SelectionMode="Single" GridLinesVisibility="Horizontal"
                  BorderBrush="#DDD" BorderThickness="1">
            <DataGrid.Columns>
                <DataGridCheckBoxColumn Header="" Binding="{Binding IsSelected, UpdateSourceTrigger=PropertyChanged}" Width="36" />
                <DataGridTextColumn Header="名称" Binding="{Binding Name}" Width="120" IsReadOnly="True" />
                <DataGridTextColumn Header="主机" Binding="{Binding Host}" Width="140" IsReadOnly="True" />
                <DataGridTextColumn Header="命令" Binding="{Binding CommandLine}" Width="*" IsReadOnly="True" />
            </DataGrid.Columns>
        </DataGrid>

        <StackPanel Grid.Row="2" Orientation="Horizontal" Margin="0,8,0,0">
            <Button Content="全选" Click="SelectAll_Click" Padding="10,3" Margin="0,0,6,0" />
            <Button Content="全不选" Click="SelectNone_Click" Padding="10,3" />
        </StackPanel>

        <StackPanel Grid.Row="3" Orientation="Horizontal" HorizontalAlignment="Right" Margin="0,12,0,0">
            <Button Content="导入" Click="Import_Click" Padding="16,4" Margin="0,0,8,0" IsDefault="True" />
            <Button Content="取消" Click="Cancel_Click" Padding="16,4" IsCancel="True" />
        </StackPanel>
    </Grid>
</Window>
