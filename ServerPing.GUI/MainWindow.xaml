<Window x:Class="ServerPing.GUI.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:vm="clr-namespace:ServerPing.GUI.ViewModels"
        mc:Ignorable="d"
        Title="ServerPing - 服务器管理" Height="500" Width="700"
        WindowStartupLocation="CenterScreen"
        Loaded="Window_Loaded" Closing="Window_Closing">

    <Window.DataContext>
        <vm:MainViewModel />
    </Window.DataContext>

    <Grid Margin="12">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="*" />
            <RowDefinition Height="Auto" />
        </Grid.RowDefinitions>

        <!-- Add Server Panel -->
        <Border Grid.Row="0" BorderBrush="#DDD" BorderThickness="1" CornerRadius="4" Padding="10" Margin="0,0,0,10">
            <Grid>
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="Auto" />
                    <ColumnDefinition Width="*" />
                    <ColumnDefinition Width="Auto" />
                    <ColumnDefinition Width="*" />
                    <ColumnDefinition Width="Auto" />
                    <ColumnDefinition Width="Auto" />
                </Grid.ColumnDefinitions>

                <TextBlock Grid.Column="0" Text="名称:" VerticalAlignment="Center" Margin="0,0,6,0" />
                <TextBox Grid.Column="1" Text="{Binding NewServerName, UpdateSourceTrigger=PropertyChanged}"
                         Margin="0,0,10,0" Padding="4,3" />

                <TextBlock Grid.Column="2" Text="地址:" VerticalAlignment="Center" Margin="0,0,6,0" />
                <TextBox Grid.Column="3" Text="{Binding NewServerHost, UpdateSourceTrigger=PropertyChanged}"
                         Margin="0,0,10,0" Padding="4,3" />

                <Button Grid.Column="4" Content="添加" Command="{Binding AddServerCommand}"
                        Padding="16,4" Margin="0,0,6,0" />
                <Button Grid.Column="5" Content="从 Terminal 导入" Command="{Binding ImportFromTerminalCommand}"
                        Padding="10,4" />
            </Grid>
        </Border>

        <!-- Server List -->
        <DataGrid Grid.Row="1" ItemsSource="{Binding Servers}" SelectedItem="{Binding SelectedServer}"
                  AutoGenerateColumns="False" IsReadOnly="True" SelectionMode="Single"
                  CanUserAddRows="False" CanUserDeleteRows="False"
                  GridLinesVisibility="Horizontal" HeadersVisibility="Column"
                  BorderBrush="#DDD" BorderThickness="1">
            <DataGrid.Columns>
                <DataGridTextColumn Header="名称" Binding="{Binding Name}" Width="120" />
                <DataGridTextColumn Header="地址" Binding="{Binding Host}" Width="150" />
                <DataGridTextColumn Header="状态" Binding="{Binding StatusText}" Width="60">
                    <DataGridTextColumn.ElementStyle>
                        <Style TargetType="TextBlock">
                            <Style.Triggers>
                                <DataTrigger Binding="{Binding StatusText}" Value="在线">
                                    <Setter Property="Foreground" Value="Green" />
                                    <Setter Property="FontWeight" Value="Bold" />
                                </DataTrigger>
                                <DataTrigger Binding="{Binding StatusText}" Value="离线">
                                    <Setter Property="Foreground" Value="Red" />
                                    <Setter Property="FontWeight" Value="Bold" />
                                </DataTrigger>
                            </Style.Triggers>
                        </Style>
                    </DataGridTextColumn.ElementStyle>
                </DataGridTextColumn>
                <DataGridTextColumn Header="最后检查" Binding="{Binding LastPingTimeText}" Width="80" />
                <DataGridCheckBoxColumn Header="启用" Binding="{Binding IsEnabled}" Width="50" IsReadOnly="True" />
                <DataGridTemplateColumn Header="操作" Width="*">
                    <DataGridTemplateColumn.CellTemplate>
                        <DataTemplate>
                            <StackPanel Orientation="Horizontal">
                                <Button Content="{Binding IsEnabled, Converter={StaticResource EnableToggleConverter}}"
                                        Command="{Binding DataContext.ToggleServerCommand, RelativeSource={RelativeSource AncestorType=DataGrid}}"
                                        CommandParameter="{Binding}" Margin="0,0,6,0" Padding="8,2" />
                                <Button Content="删除"
                                        Command="{Binding DataContext.RemoveServerCommand, RelativeSource={RelativeSource AncestorType=DataGrid}}"
                                        CommandParameter="{Binding}" Padding="8,2" Foreground="Red" />
                            </StackPanel>
                        </DataTemplate>
                    </DataGridTemplateColumn.CellTemplate>
                </DataGridTemplateColumn>
            </DataGrid.Columns>
        </DataGrid>

        <!-- Status Bar -->
        <Border Grid.Row="2" Margin="0,8,0,0" Padding="6,4" Background="#F5F5F5" CornerRadius="3">
            <Grid>
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="*" />
                    <ColumnDefinition Width="Auto" />
                </Grid.ColumnDefinitions>
                <TextBlock Grid.Column="0" Text="{Binding StatusMessage}" VerticalAlignment="Center" />
                <Button Grid.Column="1" Content="刷新" Command="{Binding RefreshCommand}" Padding="10,2" />
            </Grid>
        </Border>
    </Grid>
</Window>
